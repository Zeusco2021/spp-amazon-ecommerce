server:
  port: 8082

spring:
  application:
    name: product-service
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/product_db
  jpa:
    hibernate:
      ddl-auto: update
  elasticsearch:
    uris: ${ELASTICSEARCH_URI:http://localhost:9200}
