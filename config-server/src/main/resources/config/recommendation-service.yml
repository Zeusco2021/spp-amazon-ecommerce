server:
  port: 8087

spring:
  application:
    name: recommendation-service
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/recommendation_db
  jpa:
    hibernate:
      ddl-auto: update
